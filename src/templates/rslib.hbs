import { defineConfig } from '@rslib/core';

const shared = {
    dts: {
        bundle: false,
    },
};

export default defineConfig({
    lib: [
        {
            ...shared,
            format: 'esm',
            output: {
                distPath: {
                    root: './dist/esm',
                },
                target: 'node',
            },
        },
        {
            ...shared,
            format: 'cjs',
            output: {
                distPath: {
                    root: './dist/cjs',
                },
                target: 'node',
            },
        },
    ],
});
